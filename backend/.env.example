# Environment
ENV=development

# Database Configuration
# Option 1: Use full DATABASE_URL (for local development with docker-compose)
_DATABASE_URL=postgresql://postgres:postgres@postgres:5432/app_db

# Option 2: Use individual components (for ECS deployment - set via CDK)
# DATABASE_HOST=postgres
# DATABASE_PORT=5432
# DATABASE_NAME=app_db
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres

# SMTP (MailHog)
SMTP_HOST=mailhog
SMTP_PORT=1025

# S3 (MinIO)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=app-bucket

# API
PROJECT_NAME=Backend API
API_V1_STR=/api/v1
